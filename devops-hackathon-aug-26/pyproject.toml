[project]
name = "devops-hackathon-aug-26"
version = "0.0.0"
description = "Workspace root for the DevOps-for-Gen-AI hackathon monorepo."
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv]
package = false
